# Rumble Sweep

Plugin audio originale JUCE ispirato al concetto di filtro low-pass ritmico con saturazione.

## Build Windows VST3

La build automatica parte a ogni push su `main` oppure manualmente dalla scheda **Actions**.

Al termine scarica l'artifact `RumbleSweep-Windows-VST3`.
